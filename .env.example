# ==========================================
# Database Configuration
# ==========================================
DATABASE_URL=postgres://postgres:postgres@localhost:5432/rust_backend

# ==========================================
# Application Configuration
# ==========================================
APP__ENVIRONMENT=development

# ==========================================
# Local JWT Configuration (Legacy - For OAuth Flow Only)
# ==========================================
# Note: These are only used for issuing local JWT tokens during OAuth callbacks.
# For API authentication, all bearer tokens must be Auth0 access tokens.
JWT_SECRET=your-super-secret-key-change-in-production
JWT_KID=v1
PREVIOUS_JWT_SECRETS=
PREVIOUS_JWT_KIDS=

# ==========================================
# Auth0 Configuration (Required for all authentication)
# ==========================================
# All authentication (email/password, Google, GitHub) now goes through Auth0.
# The backend validates Auth0 access tokens and does not generate its own JWTs
# for API authentication.

# Auth0 tenant domain (e.g., your-tenant.us.auth0.com)
AUTH0_DOMAIN=
# API audience configured in Auth0 (e.g., https://api.your-app.example)
AUTH0_AUDIENCE=
# Issuer URL (optional, derived from AUTH0_DOMAIN if not set)
AUTH0_ISSUER=
# JWKS cache TTL in seconds (optional, default: 3600)
AUTH0_JWKS_CACHE_TTL_SECS=3600

# Auth0 Database Connection Configuration (for email/password signup/login)
# The Client ID for your Auth0 application with Database Connection enabled
AUTH0_CLIENT_ID=
# The Client Secret for your Auth0 application (required for password grant flow)
AUTH0_CLIENT_SECRET=
# The Auth0 connection name for email/password authentication
# Default: "Username-Password-Authentication"
AUTH0_CONNECTION=Username-Password-Authentication

# ==========================================
# Security Configuration
# ==========================================
SECURITY__CORS_ALLOWED_ORIGINS=["http://localhost:3000"]
SECURITY__METRICS_ALLOW_PRIVATE_ONLY=true
SECURITY__METRICS_ADMIN_TOKEN=
SECURITY__LOGIN_MAX_FAILURES=5
SECURITY__LOGIN_LOCKOUT_SECONDS=300
SECURITY__LOGIN_BACKOFF_BASE_MS=200

# ==========================================
# OAuth Configuration (Direct to Auth0)
# ==========================================
# Note: Google and GitHub OAuth is now handled directly through Auth0,
# not via local OAuth implementation. These values are only kept for
# reference and may be removed in future versions.

GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ==========================================
# Logging Configuration
# ==========================================
RUST_LOG=debug
